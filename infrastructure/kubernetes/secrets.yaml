apiVersion: v1
kind: Secret
metadata:
  name: sentinel-secrets
  namespace: sentinel
type: Opaque
stringData:
  database-url: "postgresql://sentinel_user:CHANGE_ME_SECURE_PASSWORD@postgres:5432/sentinel_shield"
  jwt-secret: "CHANGE_ME_RANDOM_JWT_SECRET_KEY_MIN_32_CHARS"
  redis-password: "CHANGE_ME_REDIS_PASSWORD"
